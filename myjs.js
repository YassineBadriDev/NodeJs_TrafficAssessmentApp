var tab = [];

function initialiserCercle( lat, lng )
{
  for ( i=0; i<2; i++ )
  {
    tab.push( 'hada' );//drawCercle(lat, lng) );
    //tab[i].addTo( map );
  }
}

initialiserCercle( 1, 1 );

console.log( tab );